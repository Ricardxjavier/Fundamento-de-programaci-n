#include <stdio.h>
#include <string.h>

void formatPhoneNumber(char *phoneNumber) {
    // Implementa la lógica para convertir y escribir el número de teléfono en el nuevo formato
    // ...
}

int main() {
    char phoneNumber[] = "123-456-7890";
    formatPhoneNumber(phoneNumber);
    printf("Número de teléfono en el nuevo formato: %s\n", phoneNumber);
    return 0;
}
